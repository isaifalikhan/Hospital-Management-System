{
  "buildCommand": "npm install -g pnpm && pnpm --dir backend install && pnpm --dir frontend install && pnpm --dir frontend run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
