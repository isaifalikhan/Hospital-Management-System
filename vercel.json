{
  "buildCommand": "corepack enable && corepack prepare pnpm@11.5.0 --activate && corepack pnpm --version && corepack pnpm --dir backend install --prod && corepack pnpm --dir frontend install && corepack pnpm --dir frontend run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.js": { "maxDuration": 60 }
  },
  "crons": [
    { "path": "/api/cron/reminders", "schedule": "0 8 * * *" }
  ]
}
